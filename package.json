{
  "name": "notyfai-cli-workspace",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "publish:cli": "npm publish -w notyfai",
    "publish:cli:dry": "npm publish -w notyfai --dry-run"
  }
}
