# notyfai-cli

Interactive setup for Notyfai Cursor hooks: `npx notyfai setup`.

Generate a CLI setup key in the Notyfai app, then run with `NOTYFAI_API_URL` and `NOTYFAI_SETUP_KEY` (see `notyfai setup --help`).
